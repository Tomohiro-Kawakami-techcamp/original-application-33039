<head>
<div class = "general">
  <h4 class = "alldata">全データ</h4>
  <div class = "backs1">
    <%= link_to "戻る", user_path(current_user.id), method: :get %>
  </div>
</div>
</head>
<div class = "page">
  <%= paginate @orders %>
</div>
<div class = "info">
<body class = "data">
<table class = "detail-table">
<tbody>
        <tr>
          <th class="detail-item9">購入者</th>
          <th class="detail-item10">購入した商品</th>
          <th class="detail-item11">購入日時</th>
        </tr>
        <% @orders.each do |order| %>
        <tr>
          <td class="detail-value9"><%= order.user.name %></td>
          <td class="detail-value10"><%= order.item.name %></td>
          <td class="detail-value11"><%= order.created_at %></td>
        </tr>
        <% end %>
      </tbody>
</table>
</body>
</div>